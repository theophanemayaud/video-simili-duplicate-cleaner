{
  "name": "Video simili duplicate cleaner",
  "version": "1.3.0",
  "description": "Video simili duplicate cleaner is a program that can find duplicate and similar video files.",
  "main": "QtProject/video-simili-duplicate-cleaner.pro",
  "devDependencies": {
    "@commitlint/cli": "^11.0.0",
    "@commitlint/config-conventional": "^11.0.0",
    "husky": "^4.3.8",
    "standard-version": "^9.3.0"
  },
  "husky": {
    "hooks": {
      "commit-msg": "commitlint -E HUSKY_GIT_PARAMS"
    }
  },
  "standard-version": {
    "scripts": {
      "postbump": "npm run binaries && git add -A"
    }
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "release": "standard-version --sign",
    "release:minor": "standard-version --release-as minor --sign && npm run binaries && npm run notif-push",
    "release:patch": "standard-version --release-as patch --sign && npm run binaries && npm run notif-push",
    "release:major": "standard-version --release-as major --sign && npm run binaries && npm run notif-push",
    "notif-push": "echo '\n\n Remember to push commits and tags with \n git push && git push --tags' ",
    "update-build-version": "node -p -e \"require('./package.json').version\" > ./QtProject/app/version.txt && npm run update-plist-version",
    "build": "npm run update-build-version && npm run build-mac-binaries --prefix QtProject/release-build",
    "clean": "npm run clean  --prefix QtProject/release-build",
    "copy-binaries": "cp 'QtProject/release-build/Video simili duplicate cleaner.dmg' '../Video simili duplicate cleaner.dmg' && cp -R 'QtProject/release-build/Video simili duplicate cleaner.app' '../' & cp 'QtProject/release-build/Video simili duplicate cleaner.pkg' '../Video simili duplicate cleaner.pkg'",
    "binaries": "npm run clean && npm run build && npm run copy-binaries && npm run clean",
    "update-plist-version": "node update-plist-version.js",
    "win-update-build-version": "node -p -e \"require('./package.json').version\" > ./QtProject/app/version.txt && node update-appxmanifest-version.js",
    "win-build": "npm run win-update-build-version && npm run build-win-binaries --prefix QtProject/release-build/windows",
    "win-clean": "npm run clean  --prefix QtProject/release-build/windows",
    "win-copy-binaries": "copy \"QtProject\\release-build\\windows\\Video simili duplicate cleaner.msix\" \"..\\Video simili duplicate cleaner.msix\"",
    "win-binaries": "npm run win-clean && npm run win-build && npm run win-copy-binaries && npm run win-clean"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/theophanemayaud/video-simili-duplicate-cleaner"
  },
  "author": "Théophane Mayaud",
  "license": "GPL-3.0",
  "bugs": {
    "url": "https://github.com/theophanemayaud/video-simili-duplicate-cleaner/issues"
  },
  "homepage": "https://github.com/theophanemayaud/video-simili-duplicate-cleaner#readme"
}
