version = 1
name = "Video Similarity Duplicate Cleaner"

# Codex sets CODEX_WORKTREE_PATH to the worktree it just created, so the setup
# script is addressed explicitly rather than through the working directory.
[setup]
script = '"$CODEX_WORKTREE_PATH"/scripts/link-build-deps.sh'
