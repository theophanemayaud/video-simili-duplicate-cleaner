version = 1
name = "Video Similarity Duplicate Cleaner"

# Codex runs this in each managed worktree after checkout. Keep the setup logic
# in the tracked script so Git, Cursor, Codex, and VS Code all behave alike.
[setup]
script = "scripts/link-build-deps.sh"
