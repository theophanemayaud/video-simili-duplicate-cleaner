{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "macos debug",
            "type": "cppdbg",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "cwd": "${command:cmake.launchTargetDirectory}",
            "MIMode": "lldb",
        },
    ]
}
